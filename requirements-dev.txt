# Compatibility dependency export. Use `uv sync --group dev` for normal
# development, adding `--group rag` only for indexing/retrieval work.
-r requirements.txt

# Tests, evaluation, quality checks
pytest>=8.0,<9.0
pytest-cov>=6.0,<7.0
ruff>=0.9,<1.0
mypy>=1.14,<2.0
