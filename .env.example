# Choose the LLM provider/configuration when the adapter is implemented.
MISTRAL_API_KEY=
MISTRAL_MODEL=

# Local, persistent ChromaDB storage (ignored by Git).
CHROMA_PERSIST_DIRECTORY=data/chromadb

