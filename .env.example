# Local Mistral runtime configuration (for example, Ollama or another local
# OpenAI-compatible endpoint). No Mistral Python SDK or cloud API key is used.
LLM_BASE_URL=
LLM_MODEL=

# Local, persistent ChromaDB storage (ignored by Git).
CHROMA_PERSIST_DIRECTORY=data/chromadb
